<!-- Content Header (Page header) -->
<div class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1 class="m-0">Quản lí sản phẩm</h1>
            </div><!-- /.col -->
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="#">Home</a></li>
                    <li class="breadcrumb-item active">Quản lí sản phẩm</li>
                </ol>
            </div><!-- /.col -->
        </div><!-- /.row -->
    </div><!-- /.container-fluid -->
</div>
<!-- /.content-header -->

<!-- Main content -->
<section class="content">
    <div class="container-fluid">
        <div class="col-md-6">

            <div class="card card-primary">
                <div class="card-header">
                    <h3 class="card-title">Chỉnh sửa sản phẩm</h3>
                </div>

                <form action="/admin/products/update/{{data._id}}?_method=put" method="post" enctype="multipart/form-data">

                    <div class="card-body">
                        <div class="form-group">
                            <label for="name">Tên sản phẩm</label>
                            <input type="text" class="form-control" name="name" id="name" placeholder="Enter name"
                                value="{{data.name}}" required>
                        </div>
                        <div class="form-group">
                            <label for="price">Giá</label>
                            <input type="number" class="form-control" id="price" name="price" placeholder="price"
                                value="{{data.price}}" required>
                        </div>
                        <div class="form-group">
                            <label for="quantity">Số lượng</label>
                            <input type="number" class="form-control" id="quantity" name="quantity"
                                value="{{data.quantity}}" placeholder="quantity" required>
                        </div>
                        {{!-- <div class="form-group">
                            <label>Brand</label>
                            <select class="form-control" name="brand_code">
                                {{#each brands}}
                                <option {{#if (eq ../data.brand_code this.code)}}selected{{/if}} value="{{this.code}}">
                                    {{this.name}}
                                </option>

                                {{/each}}
                            </select>
                        </div> --}}
                        <div class="form-group">
                            <div class="form-group">
                                <label for="image">Chọn ảnh</label>
                                <input type="file" class="form-control-file" id="image" name="image">
                                <img src="{{data.image}}" alt="" width="150" height="150" class="mt-3">
                            </div>

                        </div>

                        {{!-- <div class="form-check">
                            <input type="checkbox" class="form-check-input" id="is_hot" name="is_hot" value="true" {{#if
                                data.is_hot}} checked {{/if}}>
                            <label class="form-check-label" for="exampleCheck1">Hot</label>
                        </div> --}}
                    </div>

                    <div class="card-footer">
                        <button type="submit" id="update-btn" class="btn btn-primary">Submit</button>
                    </div>

                </form>

            </div>

        </div>

    </div>
    </div><!-- /.container-fluid -->
</section>
<!-- /.content -->